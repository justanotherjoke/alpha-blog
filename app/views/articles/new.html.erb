<h1>Create a new article</h1>

<!--
the scope is article, url defines where this submits it to
the last part (local: ture) is to post this form using standard http post instead of remotely using ajax
-->
<%= form_with scope: :article, url: articles_path, local: true do |f| %>
  <p>
    <%= f.label :title %><br/>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :description %></br>
    <%= f.text_area :description %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>